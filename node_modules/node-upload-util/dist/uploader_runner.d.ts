import { Uploader } from './interface/common';
export declare class UploaderRunner {
    private uploaderMap;
    constructor();
    register(name: string, uploader: Uploader): void;
    unregister(name: string): void;
    start(): Promise<void>;
}
